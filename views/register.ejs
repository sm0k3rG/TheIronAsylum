<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="resources/css/style.css">
    <title>Registrar</title>
</head>
<body>
    <div class="register-form">
		<h1>Registrar nuevo usuario</h1>
		<form action="/register" method="POST">
            <label for="user">Nombre de usuario</label>
                <input type="text" name="user" required tabindex="1" autofocus>           
            <label for="email">Correo electrónico</label>
                <input type="text" name="email" required tabindex="2">
            <label for="edad">Edad</label>
            <br>
                <input type="number" step="1" name="edad" min="1" max="100" required tabindex="1" autofocus>
                <br>
            <label for="estado">estado</label>
                <input type="text" pattern="[01]" name="estado" placeholder="Ingresar 0 si esta inactivo o 1 si esta activo">
            
            <label for="rol">Rol</label>                          
            <select id="rol" name="rol" class="select-css" tabindex="3">
                <option value="admin" selected>Admin</option>
                <option value="usuario">Usuario</option> 
                <option value="entrenador">Entrenador</option>    

            </select>
             <br> 
            <label for="pass">Contraseña</label>
			    <input type="password" name="pass" required tabindex="4">
            <div style="display: inline;">             
                <a href="/" class="btn-cancel">Cancelar</a><input type="submit" class="btn-save" value="Guardar">           
            </div>
		</form>		
	</div>

    		
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

<% if (typeof alert != "undefined")  { %>
 <script>
    Swal.fire({
        title:'<%= alertTitle %>',
        text:'<%= alertMessage %>',
        icon:'<%= alertIcon %>',
        showConfirmButton: <%= showConfirmButton %>,
        timer: <%= timer %>
      }).then(()=>{
          window.location='/<%= ruta %>'
    })
    </script>
    <% } %>
  </div>
</body>
</html>
